# 🚀 План улучшений: GSAP интеграция

## 📋 Обзор

Этот документ описывает план улучшений для:
1. **`useSimpleGSAPCarousel`** - расширение функциональности хука карусели
2. **Страница Contacts** - миграция с Framer Motion на GSAP для лучшей производительности

---

## 🎯 Часть 1: Улучшения `useSimpleGSAPCarousel`

### 🔴 Критичные улучшения (Must Have)

#### 1.1 Поддержка Snap к карточкам
**Проблема:** Текущая реализация просто скроллит без остановки на карточках  
**Решение:**
- Добавить параметр `snapToCards?: boolean`
- Вычислять ближайшую карточку при завершении momentum
- Использовать `gsap.to()` с `snap` для плавного snap
- Добавить callback `onCardChange?: (index: number) => void`

**Приоритет:** 🔴 Высокий  
**Сложность:** Средняя  
**Время:** 2-3 часа

---

#### 1.2 Ограничение границ (Bounds Checking)
**Проблема:** Карусель может уехать в бесконечность  
**Решение:**
- Вычислять `minX` и `maxX` на основе ширины контейнера и карточек
- Использовать `gsap.utils.clamp()` для ограничения позиции
- Добавить "резиновый" эффект при достижении границ (rubber band effect)

**Приоритет:** 🔴 Высокий  
**Сложность:** Низкая  
**Время:** 1-2 часа

---

#### 1.3 Поддержка Mouse Wheel (большие дельты)
**Проблема:** Текущий код обрабатывает только trackpad (малые дельты ≤ 6px)  
**Решение:**
- Добавить обработку больших дельт для обычной мыши
- Использовать `gsap.to()` вместо `gsap.set()` для плавной анимации
- Настраиваемая чувствительность через параметр `sensitivity?: number`

**Приоритет:** 🔴 Высокий  
**Сложность:** Низкая  
**Время:** 1 час

---

#### 1.4 Индексация и отслеживание текущей карточки
**Проблема:** Нет способа узнать, какая карточка сейчас активна  
**Решение:**
- Добавить `currentIndex` в возвращаемое значение хука
- Вычислять индекс на основе позиции `x`
- Обновлять индекс при snap или изменении позиции

**Приоритет:** 🔴 Высокий  
**Сложность:** Низкая  
**Время:** 1 час

---

### 🟡 Важные улучшения (Should Have)

#### 1.5 Поддержка Touch событий
**Проблема:** На мобильных устройствах карусель не работает  
**Решение:**
- Добавить обработчики `touchstart`, `touchmove`, `touchend`
- Реализовать swipe detection с velocity
- Использовать `gsap.to()` для momentum после swipe
- Добавить параметр `enableTouch?: boolean`

**Приоритет:** 🟡 Средний  
**Сложность:** Средняя  
**Время:** 3-4 часа

---

#### 1.6 Улучшенная momentum анимация
**Проблема:** Текущая реализация использует простой exponential decay  
**Решение:**
- Использовать `gsap.to()` с `ease: "power2.out"` или `ease: "power3.out"`
- Или использовать `gsap.quickTo()` для более плавной анимации
- Добавить настраиваемый параметр `momentumEase?: string`

**Приоритет:** 🟡 Средний  
**Сложность:** Низкая  
**Время:** 1 час

---

#### 1.7 Программное управление (API методы)
**Проблема:** Нет способа программно переключить карточку  
**Решение:**
- Добавить методы в возвращаемое значение:
  - `goTo(index: number)` - перейти к карточке
  - `next()` - следующая карточка
  - `prev()` - предыдущая карточка
  - `canGoNext(): boolean` - можно ли идти дальше
  - `canGoPrev(): boolean` - можно ли идти назад

**Приоритет:** 🟡 Средний  
**Сложность:** Низкая  
**Время:** 2 часа

---

#### 1.8 Оптимизация производительности
**Проблема:** Множественные `setTimeout` и `requestAnimationFrame`  
**Решение:**
- Использовать `gsap.ticker` для единого цикла анимации
- Debounce wheel events через `gsap.utils.throttle()`
- Использовать `will-change: transform` в CSS (уже есть)
- Добавить `force3D: true` в gsap для GPU acceleration

**Приоритет:** 🟡 Средний  
**Сложность:** Средняя  
**Время:** 2-3 часа

---

### 🟢 Дополнительные улучшения (Nice to Have)

#### 1.9 Параллельная прокрутка нескольких каруселей
**Проблема:** Если на странице несколько каруселей, они конфликтуют  
**Решение:**
- Добавить `id` или `group` для синхронизации
- Использовать `gsap.context()` для изоляции анимаций

**Приоритет:** 🟢 Низкий  
**Сложность:** Средняя  
**Время:** 2 часа

---

#### 1.10 Автопрокрутка
**Проблема:** Нет опции автоматической прокрутки  
**Решение:**
- Добавить параметр `autoPlay?: { interval: number, pauseOnHover?: boolean }`
- Использовать `gsap.delayedCall()` для интервалов

**Приоритет:** 🟢 Низкий  
**Сложность:** Низкая  
**Время:** 1-2 часа

---

#### 1.11 Lazy loading карточек
**Проблема:** Все карточки рендерятся сразу  
**Решение:**
- Определять видимые карточки через `IntersectionObserver`
- Использовать `gsap.set()` для предзагрузки соседних карточек

**Приоритет:** 🟢 Низкий  
**Сложность:** Высокая  
**Время:** 4-5 часов

---

## 🎯 Часть 2: Миграция Contacts Page на GSAP

### 🔴 Критичные задачи

#### 2.1 Замена Framer Motion на GSAP для page transitions
**Текущее состояние:**
```tsx
<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
  exit={{ opacity: 0 }}
>
```

**Решение:**
- Создать хук `useGSAPPageTransition`
- Использовать `gsap.fromTo()` для fade in/out
- Интегрировать с React Router для exit animations
- Использовать `gsap.context()` для cleanup

**Приоритет:** 🔴 Высокий  
**Сложность:** Средняя  
**Время:** 2-3 часа

**Файлы для изменения:**
- `frontend/src/pages/Contacts/index.tsx`
- Создать `frontend/src/hooks/useGSAPPageTransition.ts`

---

#### 2.2 Stagger анимация для элементов страницы
**Текущее состояние:**
```tsx
<motion.h1
  initial={{ opacity: 0, y: -20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: 0.2 }}
>
```

**Решение:**
- Использовать `gsap.from()` с `stagger` для последовательной анимации
- Создать хук `useGSAPStagger` для переиспользования
- Анимировать: заголовок, диал, контакты, кнопки

**Приоритет:** 🔴 Высокий  
**Сложность:** Низкая  
**Время:** 1-2 часа

**Пример:**
```typescript
gsap.from('.contact-element', {
  opacity: 0,
  y: 20,
  duration: 0.6,
  stagger: 0.1,
  ease: 'power2.out'
})
```

---

#### 2.3 Анимация Footer Drawer через GSAP
**Текущее состояние:** CSS transitions с `--footer-progress`  
**Проблема:** Менее гибко, сложно добавить spring physics

**Решение:**
- Заменить CSS transitions на `gsap.to()`
- Использовать `gsap.quickTo()` для плавного следования за жестами
- Добавить spring easing через `gsap.registerEase()` или плагин `InertiaPlugin`
- Улучшить производительность через GPU acceleration

**Приоритет:** 🔴 Высокий  
**Сложность:** Средняя  
**Время:** 3-4 часа

**Файлы для изменения:**
- `frontend/src/pages/Contacts/index.tsx`
- `frontend/src/pages/Contacts/Contacts.module.css` (убрать transitions)

---

### 🟡 Важные задачи

#### 2.4 Анимация RotaryDial через GSAP Spring
**Текущее состояние:** Кастомная spring physics через `requestAnimationFrame`  
**Проблема:** Можно использовать GSAP для более плавной анимации

**Решение:**
- Заменить кастомный spring на `gsap.to()` с `ease: "elastic.out(1, 0.3)"`
- Или использовать плагин `CustomEase` для точной настройки
- Упростить код, убрав ручной `requestAnimationFrame`

**Приоритет:** 🟡 Средний  
**Сложность:** Средняя  
**Время:** 2-3 часа

**Файлы для изменения:**
- `frontend/src/components/RotaryDialCircular/RotaryDialCircular.tsx`

**Преимущества:**
- Меньше кода
- Лучшая производительность
- Легче настраивать параметры

---

#### 2.5 Анимация FeedbackForm модального окна
**Текущее состояние:** Использует Framer Motion `AnimatePresence`

**Решение:**
- Заменить на GSAP анимации
- Использовать `gsap.fromTo()` для появления/исчезновения
- Добавить backdrop fade animation
- Использовать `gsap.context()` для изоляции

**Приоритет:** 🟡 Средний  
**Сложность:** Низкая  
**Время:** 1-2 часа

**Файлы для изменения:**
- `frontend/src/components/FeedbackForm/FeedbackForm.tsx`

---

#### 2.6 Hover анимации для интерактивных элементов
**Текущее состояние:** CSS transitions

**Решение:**
- Заменить на GSAP для более плавных анимаций
- Использовать `gsap.to()` на hover
- Добавить микро-анимации (scale, glow effects)
- Использовать `gsap.quickTo()` для следования за курсором (если нужно)

**Приоритет:** 🟡 Средний  
**Сложность:** Низкая  
**Время:** 1-2 часа

**Элементы:**
- Кнопка "Обратная связь"
- Номер телефона (hover effect)
- Иконки соцсетей

---

### 🟢 Дополнительные задачи

#### 2.7 Parallax эффекты (опционально)
**Решение:**
- Добавить subtle parallax для заголовка при скролле
- Использовать `gsap.to()` с `scrollTrigger` (если нужен скролл)
- Или использовать `gsap.to()` с `mouse` для следования за курсором

**Приоритет:** 🟢 Низкий  
**Сложность:** Средняя  
**Время:** 2-3 часа

---

#### 2.8 Анимация появления элементов при скролле
**Решение:**
- Использовать `ScrollTrigger` плагин GSAP
- Анимировать элементы при входе в viewport
- Добавить fade + slide up эффекты

**Приоритет:** 🟢 Низкий  
**Сложность:** Средняя  
**Время:** 2-3 часа

**Примечание:** Требует установки `gsap/ScrollTrigger`

---

## 📦 Дополнительные зависимости

### Обязательные
- ✅ `gsap` - уже установлен

### Опциональные (для расширенной функциональности)
- `gsap/ScrollTrigger` - для scroll-based анимаций (если нужен пункт 2.8)
- `gsap/InertiaPlugin` - для инерции в карусели (опционально)
- `gsap/CustomEase` - для кастомных easing функций (опционально)

**Установка:**
```bash
npm install gsap
# Опционально:
# npm install @gsap/react (если нужны React хуки от GSAP)
```

---

## 🎨 Рекомендуемые GSAP паттерны

### 1. Использование `gsap.context()` для cleanup
```typescript
useEffect(() => {
  const ctx = gsap.context(() => {
    gsap.to('.element', { x: 100 })
  }, containerRef)
  
  return () => ctx.revert()
}, [])
```

### 2. Использование `gsap.quickTo()` для плавных интерактивных анимаций
```typescript
const quickTo = gsap.quickTo(element, 'x', { duration: 0.3, ease: 'power1.out' })
quickTo(100) // Плавно двигается к 100
```

### 3. Использование `gsap.utils` для утилит
```typescript
const clamped = gsap.utils.clamp(0, 100, value)
const mapped = gsap.utils.mapRange(0, 100, 0, 1, value)
```

---

## 📊 Приоритизация задач

### Фаза 1: Критичные улучшения (1-2 недели)
1. ✅ Snap к карточкам (1.1)
2. ✅ Bounds checking (1.2)
3. ✅ Mouse wheel support (1.3)
4. ✅ Индексация карточек (1.4)
5. ✅ Page transitions на GSAP (2.1)
6. ✅ Stagger анимации (2.2)
7. ✅ Footer drawer через GSAP (2.3)

### Фаза 2: Важные улучшения (1-2 недели)
8. ✅ Touch support (1.5)
9. ✅ Улучшенная momentum (1.6)
10. ✅ API методы (1.7)
11. ✅ RotaryDial через GSAP (2.4)
12. ✅ FeedbackForm анимации (2.5)
13. ✅ Hover анимации (2.6)

### Фаза 3: Оптимизация и дополнения (по необходимости)
14. Оптимизация производительности (1.8)
15. Автопрокрутка (1.10)
16. Parallax эффекты (2.7)
17. Scroll-based анимации (2.8)

---

## 🧪 Тестирование

### Что тестировать:
- ✅ Карусель работает на desktop (mouse wheel)
- ✅ Карусель работает на trackpad (momentum)
- ✅ Карусель работает на mobile (touch)
- ✅ Snap к карточкам работает корректно
- ✅ Границы соблюдаются
- ✅ Page transitions плавные
- ✅ Footer drawer анимация плавная
- ✅ Нет memory leaks (проверить cleanup)
- ✅ Производительность 60fps

---

## 📝 Заметки

### Преимущества GSAP над Framer Motion:
- ✅ Лучшая производительность (особенно на мобильных)
- ✅ Меньший bundle size (если использовать только нужные части)
- ✅ Больше контроля над анимациями
- ✅ Лучшая поддержка сложных анимаций
- ✅ Отличная документация и community

### Когда оставить Framer Motion:
- Если уже используется для layout animations
- Если нужны gesture-based анимации (drag, pan)
- Если команда уже знакома с Framer Motion

---

## 🚀 Быстрый старт

### Шаг 1: Улучшить карусель
```bash
# Начать с критичных улучшений
# 1. Добавить snap
# 2. Добавить bounds
# 3. Добавить mouse wheel
```

### Шаг 2: Мигрировать Contacts page
```bash
# 1. Создать useGSAPPageTransition
# 2. Заменить motion.div на обычный div
# 3. Добавить stagger анимации
# 4. Заменить footer drawer на GSAP
```

### Шаг 3: Оптимизировать
```bash
# 1. Проверить производительность
# 2. Добавить cleanup
# 3. Оптимизировать re-renders
```

---

**Дата создания:** 2024  
**Статус:** 📋 План готов к реализации  
**Версия:** 1.0

